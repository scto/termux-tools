#!/bin/sh

if [ $# != 0 ]; then
	echo 'usage: termux-wake-unlock'
	echo 'Release the Termux wake lock to allow the CPU to sleep.'
	exit 1
fi

case "${TERMUX__USER_ID:-}" in ''|*[!0-9]*|0[0-9]*) TERMUX__USER_ID=0;; esac

am startservice \
	--user "$TERMUX__USER_ID" \
	-a com.icst.android.appstudio.service_wake_unlock \
	com.icst.android.appstudio/com.icst.android.appstudio.app.TermuxService \
	> /dev/null
